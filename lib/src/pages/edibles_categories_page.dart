import 'package:flutter/material.dart';
import '../widgets/edibles_products.dart';

class EdiblesCategoriesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 320.0,
      child: EdiblesProducts(),
    );
  }
}
